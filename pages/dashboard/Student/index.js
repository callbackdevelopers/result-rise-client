import StudentLayout from "../../../Layout/StudentLayout";

const index = () => {
    return (
        <StudentLayout>
            <div>Student Dashboard</div>
        </StudentLayout>
    );
};

export default index;
